<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.client.profile.dao.ProfileDAO">
	
	<!-- 해당 번호의 비밀번호 확인 -->
	<select id="loginCheck" resultType="String">
		select name
		from profile
		where id=#{id} and passwd=#{passwd}
	</select>
	
	<!-- 회원 리스트 기본 조회 -->
	<select id="MyPage" parameterType="profile" resultType="profile">
		SELECT *
		FROM profile
	</select>
	
	
	
	<!-- 01. 회원록록 -->
    <select id="profileList" resultType="com.spring.client.profile.vo.ProfileVO">
        SELECT 
          id AS userId, 
          name AS userName,
          email AS userEmail
        FROM profile
        ORDER BY userId
    </select>
    
    <!-- 02. 회원 등록 -->
    <insert id="insertProfile">
        INSERT INTO tbl_profile
            (id, passwd, name, email)
        VALUES 
            (#{userId}, #{userPw}, #{userName}, #{userEmail})
    </insert>
    
    <!-- 03. 회원정보 상세조회 -->
    <select id="profileView" resultType="com.spring.client.profile.vo.ProfileVO">
        SELECT 
          id AS userId, 
          name AS userName,
          email AS userEmail
        FROM profile
        WHERE id = #{userId}
    </select>
</mapper>
